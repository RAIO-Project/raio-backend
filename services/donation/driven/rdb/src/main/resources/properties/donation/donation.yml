spring:
  flyway:
    enabled: false  # (중요) 기본 Flyway Auto-run 끄고, 우리가 만든 authFlyway만 실행

donation:
  flyway:
    locations: classpath:db/migration/v1/donation
    schema: donation
    history-table: flyway_schema_history_donation