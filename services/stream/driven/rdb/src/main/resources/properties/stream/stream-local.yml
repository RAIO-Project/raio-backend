stream:
  flyway:
    create-schemas: true
    baseline-on-migrate: true
    validate-on-migrate: false
    out-of-order: true

app:
  storage:
    s3:
      endpoint: http://localhost:9000
      region: us-east-1
      access-key: minioadmin
      secret-key: minioadmin
      bucket: raio-local
      public-base-url: http://localhost:9000/raio-local